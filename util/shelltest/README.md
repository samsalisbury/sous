# Shelltest

This package wraps `testing` and provides an interface to run commands in a
persistent shell.

## Testing

Testing this is tricky. Its tests (at the moment) fail on purpose - run with
-tags=shelltesttest to get them
