#!/bin/sh

NAME=/tmp/sous

echo BUILDING
time go build -o $NAME || { echo "BUILD FAILED"; exit 1; }

echo TESTING
time go test ./... || { echo "TESTS FAILED"; exit 1; }

echo RUNNING
time $NAME $@ 

rm $NAME

